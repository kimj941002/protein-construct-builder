# Stage 3 — Reflex 앱 전용 의존성 (Streamlit requirements.txt 와 분리 관리)
# 서비스 계층(database.py, db_config.py)만 재사용하므로 streamlit/aggrid 불필요.
reflex>=0.9
sqlalchemy>=2.0
psycopg2-binary>=2.9
anthropic>=0.40.0   # 논문 분석(Phase 2) + AI 질의
PyMuPDF>=1.24.0     # 논문 PDF 텍스트/이미지 추출(Phase 2)
requests>=2.28.0    # fetcher (단백질 수집)
requests-cache>=1.0.0
# AG Grid 표는 자체 래퍼(pcb_reflex/ag_grid_wrap.py)가 npm의 ag-grid-react / ag-grid-community 를
# 자동 설치한다 — 추가 파이썬 패키지·계정 로그인 불필요.
