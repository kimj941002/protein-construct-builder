# 데이터베이스 — Turso(libSQL)로 원격 저장하므로 로컬 DB는 추적하지 않음
protein_data.db

# API 캐시 파일
protein_api_cache.sqlite

# AI 채팅 기록 (런타임 생성 파일 — GitHub에 올리지 말 것)
chat_history.json

# Python 캐시
__pycache__/
*.py[cod]
*$py.class

# 환경 설정 (로컬 전용 — 절대 GitHub에 올리지 말 것)
.env
*.env

# Streamlit Secrets (실제 키 파일 — 절대 GitHub에 올리지 말 것)
.streamlit/secrets.toml

# VS Code 설정
.vscode/

# macOS 시스템 파일
.DS_Store

# 논문 PDF (저작권 — GitHub에 올리지 말 것)
papers/*.pdf
