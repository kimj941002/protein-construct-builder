.states
*.db
.web
assets/external/
# 데이터베이스 — 단일 진실의 원천은 Supabase(Postgres). (UNIFIED_MIGRATION_PLAN.md)
# protein_data.db 는 Supabase 이전 완료 후 추적 제외 (로컬 원본 백업으로만 보관).
protein_data.db

# API 캐시 파일
protein_api_cache.sqlite

# AI 채팅 기록 (런타임 생성 파일 — GitHub에 올리지 말 것)
chat_history.json

# Python 캐시
__pycache__/
*.py[cod]
*$py.class

# Reflex 빌드 산출물 (Stage 3)
.web/
.states/
reflex.lock/
*.reflex.json

# 환경 설정 (로컬 전용 — 절대 GitHub에 올리지 말 것)
.env
*.env

# Streamlit Secrets (실제 키 파일 — 절대 GitHub에 올리지 말 것)
.streamlit/secrets.toml

# VS Code 설정
.vscode/

# macOS 시스템 파일
.DS_Store

# 논문 PDF (저작권 — GitHub에 올리지 말 것)
papers/*.pdf

# 로컬 preview 실행 설정 (머신별 절대경로)
.claude/launch.json
